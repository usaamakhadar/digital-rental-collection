# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: leases\lease.spec.ts >> Lease Agreement Management >> User can create a lease and then terminate it, verifying unit status updates
- Location: tests\leases\lease.spec.ts:5:7

# Error details

```
Error: expect(locator).toBeHidden() failed

Locator:  locator('text=Tenant 1779733522865')
Expected: hidden
Received: visible
Timeout:  15000ms

Call log:
  - Expect "toBeHidden" with timeout 15000ms
  - waiting for locator('text=Tenant 1779733522865')
    33 × locator resolved to <span>Tenant 1779733522865</span>
       - unexpected value "visible"

```

```yaml
- text: Tenant 1779733522865
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { loginUser, createTestProperty, createTestUnit } from '../helpers/test-utils';
  3  | 
  4  | test.describe('Lease Agreement Management', () => {
  5  |   test('User can create a lease and then terminate it, verifying unit status updates', async ({ page }) => {
  6  |     test.setTimeout(90000);
  7  |     // Log in
  8  |     await loginUser(page, 'muscabkhadar69@gmail.com', '123456');
  9  | 
  10 |     const timestamp = Date.now();
  11 |     const propertyName = `PropLease ${timestamp}`;
  12 |     const unitNumber = `UnitLease ${timestamp}`;
  13 |     const tenantName = `Tenant ${timestamp}`;
  14 | 
  15 |     // Register a global dialog handler to automatically accept all alerts/confirms
  16 |     page.on('dialog', async dialog => {
  17 |       console.log(`[Dialog] ${dialog.message()}`);
  18 |       await dialog.accept();
  19 |     });
  20 | 
  21 |     // Create Property and vacant Unit
  22 |     await createTestProperty(page, propertyName);
  23 |     await createTestUnit(page, propertyName, unitNumber, '400');
  24 | 
  25 |     // Locate the tenant leasing form using the presence of select[name="unitId"]
  26 |     const leaseForm = page.locator('form').filter({ has: page.locator('select[name="unitId"]') });
  27 |     
  28 |     // Fill lease form details
  29 |     await leaseForm.locator('input[name="name"]').fill(tenantName);
  30 |     await leaseForm.locator('input[name="phone"]').fill('634463028');
  31 |     await leaseForm.locator('input[name="emergencyContactName"]').fill('Damiin Test');
  32 |     await leaseForm.locator('input[name="emergencyContactPhone"]').fill('636699669');
  33 | 
  34 |     // Select the vacant unit from dropdown
  35 |     await leaseForm.locator('select[name="unitId"]').selectOption({ label: `${propertyName} - ${unitNumber} ($400.00)` });
  36 | 
  37 |     // Set lease start date
  38 |     await leaseForm.locator('input[name="startDate"]').fill('2026-05-25');
  39 | 
  40 |     // Create Lease
  41 |     await leaseForm.locator('button[type="submit"]').click();
  42 | 
  43 |     // Click Kiraystayaasha tab to view the full active leases list
  44 |     await page.locator('button:has-text("Kiraystayaasha")').click();
  45 | 
  46 |     // Verify tenant name is listed in Active Leases table (reload if necessary due to cache)
  47 |     await expect(page.locator(`text=${tenantName}`)).toBeVisible({ timeout: 5000 }).catch(async () => {
  48 |       await page.reload();
  49 |       await page.locator('button:has-text("Kiraystayaasha")').click();
  50 |       await expect(page.locator(`text=${tenantName}`)).toBeVisible({ timeout: 10000 });
  51 |     });
  52 | 
  53 |     // Click Guryaha to verify unit status is now occupied (La Degan Yahay)
  54 |     const propertiesTab = page.locator('button:has-text("Guryaha")');
  55 |     await propertiesTab.click();
  56 |     const unitRow = page.locator('div.flex.justify-between.items-center').filter({ hasText: unitNumber }).first();
  57 |     await expect(unitRow.locator('text=La Degan Yahay')).toBeVisible();
  58 | 
  59 |     // Click Kiraystayaasha tab again to return to the active leases list
  60 |     await page.locator('button:has-text("Kiraystayaasha")').click();
  61 | 
  62 |     // Locate and click "Jooji Kiro" for this tenant
  63 |     const leaseRow = page.locator(`tr:has-text("${tenantName}")`);
  64 |     await leaseRow.locator('button:has-text("Jooji Kiro")').click();
  65 | 
  66 |     // Verify tenant is no longer in Active Leases table
> 67 |     await expect(page.locator(`text=${tenantName}`)).toBeHidden({ timeout: 15000 });
     |                                                      ^ Error: expect(locator).toBeHidden() failed
  68 | 
  69 |     // Verify unit status reverted to vacant (Bannaan)
  70 |     await propertiesTab.click();
  71 |     const unitRowVacant = page.locator('div.flex.justify-between.items-center').filter({ hasText: unitNumber }).first();
  72 |     await expect(unitRowVacant.locator('text=Bannaan')).toBeVisible();
  73 |   });
  74 | });
  75 | 
```